step 1: Build textarea input and have the input displayed on the UI above input should
have button to submmit the input and display it above the text area on the output area.
use a send button that use send icon
step 2 : use language detector API to get the text input language and render it below the output text
step 3: summarizing after 150 characters use the summarize API
step 4: translate text to other languages 
 